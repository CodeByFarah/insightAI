# The backend reads .env from the repository root first, then from backend/.
# See ../.env.example for the full list of settings.
DATABASE_URL=postgresql+psycopg://insightai:insightai@localhost:5432/insightai
GEMINI_API_KEY=
